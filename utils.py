import sys

from PyQt5.QtGui import *
from PyQt5.QtWidgets import *

def create_analysis_widget(descript, data):
    tmp = str(descript) + ': ' + str(data)
    text = QLabel(tmp)

def


'''
def create_analysis_widget(descript, data):
    tmp = str(descript) + ': ' + str(data)
    text = QTextLine(QTextLine=tmp)
    return text
'''
